# 잎 표면 맵 시리즈 — 맵핑 데이터 + 대표 스펙트럼 (논문 양식)

**작업일** 2026-08-15 · **입력** 7맵

★ 가 지금 **그림에 들어가는** 맵이다. 나머지도 전처리·판독을 거쳐 CSV 에 들어 있다.
대조군 `cov3` 는 맵 그림에는 안 나오고 `Fig_leaf_contrast` 에서 blank 로만 쓴다.

| 그룹 | 맵 | 격자 |
|---|---|---|
| 단일 성분 | `leaf_dq` · `leaf_tb1` · `leaf_thi2` | 10×10, 50 µm 스텝 (450 µm) |
| **대조군** | `cov3` — 잎에 **잉크만** (blank 전용) | 10×10, 50 µm 스텝 (450 µm) |
| 혼합 | ★ `leavesmix` — peel 전 | 20×20, 100 µm 스텝 (1900 µm) |
| 혼합 | ★ `leavesmix2peel` — peel 후 | 20×20, 100 µm 스텝 (1900 µm) |
| 혼합 | `leavesmix2peel2` | 10×10, 50 µm 스텝 (450 µm) |

논문에 바로 붙일 **그림 4장**과 Origin 으로 다시 그릴 **tidy CSV** 를 뽑았다.
**그림에는 `leavesmix`(peel 전)와 `leavesmix2peel`(peel 후) 두 맵만 그린다** (2026-08-15
결정). 둘 다 20×20 / 1900 µm 라 **면적이 같아서** peel 전후를 그대로 비교할 수 있다.
나머지 5맵도 전처리·판독은 그대로 돌아가고 CSV 에 전부 들어 있으니, 다시 넣으려면
`scripts/02_figures.py` 의 `SAMPLES` 목록만 늘리면 된다.
형식은 `analysis/dq9-sus-reproducibility` 와 동일하다 — `ui_common.py` 팔레트
(DQ `#1a73e8` 파랑 · TBZ `#4a9e2a` 초록 · THI `#c85a8f` 핑크), Helvetica 8.5 pt,
축 안에 글자 없음(범례·시료 라벨은 축 바깥), 벡터 PDF(Type42) + 400 dpi PNG.

---

## 1. 그림

| 파일 | 내용 |
|---|---|
| **`figures/Fig_leaf_panel.pdf` / `.png`** | **슬라이드용 합본** — 맵(행=시료·열=판독값) 위, 대표 스펙트럼 아래 (7.2 × 6.6 in) |
| `figures/Fig_leaf_maps.pdf` / `.png` | 4 판독값 × 2 열 = 8 패널 히트맵 (4.9 × 5.35 in) |
| `figures/Fig_leaf_spectra.pdf` / `.png` | 대표 스펙트럼 2패널 (7.2 × 4.2 in) |
| `figures/Fig_leaf_contrast.pdf` / `.png` | 대조군 대비 — 차이 스펙트럼 + 픽셀 검출률 (7.2 × 3.8 in) |
| `figures/CAPTION.md` | 네 그림의 캡션 원문 |

`Fig_leaf_panel` — 슬라이드에 그대로 들어가는 한 장 (2026-08-15 요청 배치). a: 맵을
**행 = 시료 / 열 = 판독값**으로 놓고 열마다 가로 컬러바를 달았다 (`Fig_leaf_maps` 를
90° 돌린 배치). b: 바로 아래 같은 폭으로 대표 스펙트럼. 세기 축은 **열 단위로 공유**한다 — 두 맵의 면적이 같으므로 peel 전후가 곧바로 읽힌다.

`Fig_leaf_maps` — 위 합본의 맵만 따로. 행이 판독값(잉크 2137 · DQ/잉크 · TBZ/잉크 ·
THI/잉크), 열이 시료다.
**세기 축은 행 단위로 공유**하므로 같은 행 안에서 두 맵을 바로 비교할 수 있고,
행끼리는 비교하면 안 된다. 두 맵은 격자·면적이 같지만(20×20 / 1900 µm) 열 머리글에
그대로 적어 둔다 — 나중에 면적이 다른 맵을 끼워 넣어도 바로 드러나게.

`Fig_leaf_spectra` — a: 지문영역, 잉크 2137 밴드 면적으로 정규화해 오프셋 적층. 실선이
맵 픽셀의 **중앙값**, 음영이 **사분위 범위**, 세로선이 마커밴드. b: 침묵영역 원 세기 —
두 맵 모두 잉크 리포터가 살아 있다.

색·대비는 "쨍하게" 요청에 맞춰 조정했다 (2026-08-15): 히트맵 램프를 흰색 → 성분색 →
같은 색 진한 톤의 3정거장으로 바꾸고, 색 스케일을 2–98 → **4–96 백분위**로 조여
높은 쪽 대비를 살렸다. 배경은 순백, 선 굵기 0.9 → 1.15 pt, 음영 alpha 0.16 → 0.24.
색상 자체(성분 3색)는 `ui_common.py` 값 그대로다.

`Fig_leaf_contrast` — a: 대조군을 뺀 차이 스펙트럼(잉크 정규화 후 차감). b: 각 맵에서
**대조군 평균 + 3 SD** 를 넘는 픽셀의 비율. 이 패널에는 대조군 줄을 같이 넣었다 —
2–4% 로 나오는 그 값이 문턱의 오경보 바닥이라 없으면 나머지 두 줄을 읽을 기준이 없다.

---

## 2. 방법

1. **베이스라인** — ALS (λ=1e5, p=0.01, 10회). `dq9-sus-reproducibility/scripts/
   01_load_preprocess.py` 와 같은 설정이라 두 분석의 숫자가 같은 축 위에 있다.
2. **잉크 리포터** — 2100–2180 cm⁻¹ 적분 (2137 cm⁻¹ 밴드). 침묵구간이라 분석물 밴드가
   섞이지 않는다.
3. **마커밴드** — 각 성분 3밴드를 ±10 cm⁻¹ 적분하고 평균.
   DQ 1179/1521/1576 · TBZ 779/1011/1549 · THI 556/1138/1365
   (`dq9-sus-reproducibility/README.md` §4 의 밴드 목록)
4. **판독값** = 마커/잉크. 같은 픽셀 안의 고정량 기준물질로 나누므로 기판 gain 이 상쇄된다
   (`documentation/RUN_ink-check_6maps.md` 의 근거).
5. **검출 판정** — 대조군 `cov3` 100픽셀의 (마커/잉크) 분포에서 평균 + 3 SD 를 문턱으로
   삼고, 각 맵에서 그 문턱을 넘는 픽셀 비율을 센다.
6. **대표 스펙트럼** = 맵 픽셀의 중앙값, 산포는 사분위 범위.

---

## 3. 숫자

### 3-1. 잉크 리포터 (맵 중앙값, 괄호는 조제 내 CV)

| 시료 | 잉크 2137 면적 |
|---|---|
| `leaf_dq` | 17 849 (**66%**) |
| `leaf_tb1` | 20 931 (23%) |
| `leaf_thi2` | 15 272 (29%) |
| `cov3` | 19 976 (26%) |
| ★ `leavesmix` | 11 122 (33%) |
| ★ `leavesmix2peel` | 8 236 (13%) |
| `leavesmix2peel2` | 13 797 (33%) |

### 3-2. 마커/잉크 — 대조군 대비 배수와 검출 픽셀 비율

굵은 값이 그 맵에서 가장 큰 성분이다. 검출률은 대조군 평균 + 3 SD 기준.
**★ 표시가 지금 그림에 들어가는 맵**이고, 나머지 줄도 CSV 에는 그대로 있다.

| 시료 | DQ 배수 (검출%) | TBZ 배수 (검출%) | THI 배수 (검출%) |
|---|---|---|---|
| `leaf_dq` | **3.1× (83%)** | 1.8× (31%) | 1.8× (30%) |
| `leaf_tb1` | 3.4× (76%) | **3.5× (94%)** | 3.0× (80%) |
| `leaf_thi2` | 1.5× (2%) | 1.1× (2%) | **3.0× (71%)** |
| `cov3` (대조군, blank) | 1.0× (4%) | 1.0× (2%) | 1.0× (3%) |
| ★ `leavesmix` | 2.5× (53%) | 2.2× (43%) | **4.0× (81%)** |
| ★ `leavesmix2peel` | 1.9× (10%) | 2.2× (36%) | **2.4× (48%)** |
| `leavesmix2peel2` | 1.5× (19%) | 1.7× (22%) | **1.8× (29%)** |

읽히는 것 넷.

- **대조군의 오경보 바닥은 2–4%** 다. 문턱이 제대로 작동한다.
- **`leaf_thi2` 가 선택성을 가장 깨끗하게 보인다** — THI 71% 인데 DQ·TBZ 는 2% 로
  대조군 수준이다.
- **혼합 맵에서 세 성분이 모두 문턱을 넘는다** (`leavesmix` 53/43/81%). 단일 성분 맵과
  대조군을 같이 놓으면 이게 혼합물 판독의 직접 증거가 된다.
- **peel 하면 신호가 준다** — 그림에 넣은 두 맵(같은 잎 같은 자리, 면적 동일)에서
  THI 81 → 48%, DQ 53 → 10%, TBZ 43 → 36%. 배수로도 THI 4.0× → 2.4×,
  DQ 2.5× → 1.9× 로 내려간다. 성분별 제거율은 §3-3.
- `leavesmix2peel2` 는 CSV 에만 있다 (19 / 22 / 29%). 면적이 1/18 이라 위 두 맵과
  같은 줄에 못 쓴다 — §4-4.

### 3-3. peel 이 걷어간 양 — 잉크와 나란히 (`data/peel_removal.csv`)

필름은 **잉크째** 떼어낸다 (사용자 확인). 그래서 잉크의 제거율이 곧 "필름이 표면을
얼마나 가져갔나"의 기준선이고, 분석물은 그 기준선 대비로 읽어야 한다.

| 판독값 | peel 전 | peel 후 | 제거율 | **잉크 대비 잔존** |
|---|---|---|---|---|
| 잉크 2137 | 11 122 | 8 236 | 25.9% | 1.000 (기준선) |
| DQ 마커 | 7 093 | 3 845 | 45.8% | 0.732 |
| TBZ 마커 | 4 684 | 3 243 | 30.8% | **0.935** |
| THI 마커 | 6 843 | 2 701 | **60.5%** | **0.533** |

- **잉크는 26% 걷혔다.** 필름이 잉크층을 부분적으로 떼어냈다는 뜻이고, 이게 기준선이다.
- **TBZ 는 잉크와 거의 같은 비율로 걷힌다** (잔존 0.935). 그래서 마커/잉크 비가
  0.3965 → 0.3962 로 **사실상 안 움직인다** — 그림의 TBZ 행 두 맵이 비슷해 보이는 게
  이것이다. "TBZ 가 안 없어졌다"가 아니라 **"잉크와 함께 실려 나갔다"** 로 읽어야 한다.
- **THI 가 가장 많이 걷힌다** (60.5%, 잉크 대비 잔존 0.533) — 잉크보다 2배 가까이
  잘 떨어진다. DQ 가 그다음(45.8%, 0.732).
- 즉 **필름은 성분을 고르게 걷지 않는다.** 잉크와 함께 실려 나가는 몫(TBZ)과 잉크보다
  더 잘 떨어지는 몫(THI·DQ)이 갈린다.

### 3-4. 이번 peel 은 고르게 걷히지 않았다 (예비 측정)

사용자 메모: 이번 건 **대충 뜯은 것**이다. 데이터도 그렇게 나온다 — 잉크 분위수를
맞대 보면 **두꺼운 쪽만 걷혔다.**

| 잉크 분위수 | peel 전 | peel 후 | 제거율 |
|---|---|---|---|
| p10 (얇은 쪽) | 8 175 | 7 147 | **12.6%** |
| p25 | 9 355 | 7 666 | 18.1% |
| p50 | 11 122 | 8 236 | 25.9% |
| p75 | 13 516 | 8 971 | 33.6% |
| p90 (두꺼운 쪽) | 16 734 | 9 760 | **41.7%** |

픽셀간 산포도 같이 눌린다 — 잉크 CV **32.9% → 13.2%**, 최대/최소 6.2배 → 2.4배.
필름이 **솟은 부분만 훑고 얇은 데는 거의 못 닿았다**는 뜻이다.

→ **§3-3 의 제거율은 "이 필름이 걷을 수 있는 양"의 하한**이다. 방법의 회수 성능으로
인용하지 말 것. 균일하게 뜯은 시료로 다시 재면 숫자가 올라갈 여지가 크다.
반대로 **성분별 차이(THI ≫ DQ > TBZ ≈ 잉크)** 는 같은 필름·같은 접촉에서 나온 것이라
뜯기의 거칠기와는 별개로 남는다.

여기서 못 가르는 것: 분석물이 **잉크에 붙어** 실려 나간 것인지, 잎 표면에서 **따로**
떨어진 것인지는 이 두 맵으로 구분되지 않는다. 가르려면 떼어낸 **필름 쪽을 직접 재야**
한다 (필름 A: capture-and-transfer 가설).

또 하나 — 두 맵의 **스테이지 좌표계가 다르다** (mix X −1242…658, peel X 166…2066).
peel 하느라 시료를 다시 올렸기 때문으로 보이며, 그래서 **픽셀 단위 전후 차감은 못 한다.**
비교는 맵 분포 수준까지가 한계다.

산포 표기는 `dq9-sus-reproducibility/README.md` §3 을 따랐다. **`n = 100` 을 수식어 없이
쓰지 말 것** — 여기 100(혹은 400)은 한 조제 안(within one preparation)의 픽셀이지 독립
반복이 아니다. `summary_stats.csv` 의 열 이름도 `within_prep_SD` 로 맞춰 놓았다.

---

## 4. 한계 — 이 데이터만으로 못 하는 것

### 4-1. 조성 분율(NNLS)은 못 낸다

순수 표준 스펙트럼(`Reference/DQ·TBZ·THI·BLK-*.csv`)이 이 세션에 없다. 그래서 분율
(mass fraction) 이 아니라 **마커밴드 상대세기**까지만 냈다. 참조 스펙트럼을 올려주면
같은 형식으로 NNLS 조성 맵을 추가할 수 있다.

### 4-2. `leaf_tb1` 은 세 성분이 다 올라간다 — 가릴 수 없다

이 맵의 지문영역 총세기가 다른 맵의 2–3배(중앙값 695 k vs 203–322 k)다. 잉크로 나눠도
모든 밴드가 같이 올라가서 DQ 76% · TBZ 94% · THI 80% 가 나온다. 표면 분석물이 실제로
많은 것인지 기판 hot-spot 인지 이 7맵만으로 못 가른다. **`leaf_tb1` 을 선택성 근거로
쓰지 말 것** — 그 역할은 `leaf_thi2` (71 / 2 / 2%) 가 한다.

### 4-3. 마커밴드 3개가 다 선택적이지는 않다

밴드 하나씩 갈라 본 값 (`data/band_selectivity.csv`, 잉크 정규화 중앙값):

| 성분 | 밴드 | `leaf_dq` | `leaf_tb1` | `leaf_thi2` | `cov3` | `mix` | `peel` | `peel2` |
|---|---|---|---|---|---|---|---|---|
| DQ | 1179 | 0.420 | 0.445 | 0.201 | 0.161 | 0.259 | 0.359 | 0.221 |
| DQ | 1521 | 0.597 | 0.508 | 0.443 | 0.278 | 0.726 | 0.748 | 0.181 |
| DQ | 1576 | 1.250 | 1.516 | 0.387 | 0.291 | 0.877 | 0.279 | 0.702 |
| TBZ | 779 | 0.182 | 0.348 | 0.105 | 0.083 | 0.332 | 0.354 | 0.157 |
| TBZ | 1011 | 0.131 | 0.173 | 0.147 | 0.147 | 0.343 | 0.342 | 0.198 |
| TBZ | 1549 | 0.634 | 1.307 | 0.350 | 0.294 | 0.513 | 0.477 | 0.495 |
| THI | 556 | 0.205 | 0.341 | **0.429** | 0.085 | 0.512 | 0.344 | 0.230 |
| THI | 1138 | 0.254 | 0.299 | **0.376** | 0.155 | 0.508 | 0.345 | 0.255 |
| THI | 1365 | 0.258 | 0.609 | 0.497 | 0.184 | 0.722 | 0.326 | 0.280 |

**대조군에도 1521 / 1549 / 1576 / 1011 이 있다** (0.15–0.29). 잉크·잎 기재 쪽 기여가
섞인 밴드다. 대조군 대비 배수로 읽으면 그 기여가 상쇄되지만, **절대값을 조성처럼 읽으면
안 된다.** 가장 선택적인 밴드는 THI 556(대조군 대비 6.0×) · TBZ 779(4.3×) ·
DQ 1576(5.2×) 이고, TBZ 1011 은 대조군과 거의 같아(1.47 vs 1.46) 쓸모가 없다.

### 4-4. `leavesmix2peel2` 는 면적이 달라 같은 줄에 못 쓴다

`leavesmix` · `leavesmix2peel` 은 20×20 / 1900 µm 인데 `leavesmix2peel2` 만
10×10 / 450 µm 로 **1/18 면적**이다. 그래서 그림에서 뺐다. 세 맵을 peel 0/1/2 차
계열로 쓰려면 마지막 맵을 **같은 면적으로 다시 재야 한다.**

그림에 쓴 두 맵은 **같은 잎 같은 자리**가 확인됐다 (사용자 확인 2026-08-15). peel 전후
비교는 그대로 써도 된다.

### 4-5. peel 맵은 잉크가 적어 비가 더 흔들린다

`leavesmix2peel` 의 잉크 세기가 8 236 으로 대조군(19 976)의 41% 다. 분모가 작아지면
마커/잉크가 픽셀 단위로 더 튄다 — `Fig_leaf_contrast` a 의 peel 트레이스가 눈에 띄게
거친 이유다. 결론은 맵 중앙값 수준에서만 쓰고 픽셀값 하나를 인용하지 말 것.

### 4-6. peel 후에는 마커/잉크 비를 "농도"로 읽으면 안 된다

잉크가 걷혀 나가므로 **두 맵 사이에서 잉크는 고정 기준물질이 아니다.** 픽셀 안에서
기판 gain 을 상쇄하는 역할은 그대로지만, 맵끼리 비교할 때 마커/잉크가 내려간 것은
"남은 잉크 한 단위당 분석물이 줄었다"는 뜻이지 표면 농도가 그만큼 줄었다는 뜻이 아니다.
peel 전후 크기를 말할 때는 **§3-3 의 절대 제거율과 잉크 기준선을 같이 쓸 것.**

---

## 5. 재현 방법

```bash
cd analysis/leaf-maps
python3 -u scripts/01_extract.py [데이터폴더]   # 기본값 ./raw · ~20 s · data/ 생성
python3 -u scripts/02_figures.py                # figures/ 생성
```

`raw/` 와 `data/*.npz` 는 git 에 넣지 않는다(원본은 Google Drive). 데이터폴더는
`*_corrected.csv` 가 들어 있는 아무 경로나 인자로 주면 된다 — 파일명 끝이
`leaf_dq_corrected.csv` 처럼 맞기만 하면 앞에 붙은 접두사는 무시한다.

- **처리할 맵**은 `01_extract.py` 의 `SAMPLES` — 지금 7맵 전부.
- **그림에 그릴 맵**은 `02_figures.py` 의 `SAMPLES` — 지금 `leavesmix` + `leavesmix2peel`.
  늘리거나 줄일 때 같은 파일의 `SHOW`·`LONG`·`SHORT`·`SCOL`·`SDASH` 도 함께 고칠 것.
  `CONTROL` 은 `SAMPLES` 에 없어도 `Fig_leaf_contrast` 가 blank 로 계속 쓴다.

색: 성분 3색은 `ui_common.py` 그대로 쓰고, 혼합 2맵은 성분색과 충돌하지 않게
**한 색상(앰버)의 명도 2단 + 선종류**로 구분한다. 어느 그림에서도 색이 유일한 부호가
아니다 — 트레이스마다 축 바깥에 이름이 붙고, 히트맵의 색은 행(판독값)이 정한다.

---

## 6. 파일

| 경로 | 내용 |
|---|---|
| `figures/Fig_leaf_panel.*` | **슬라이드용 합본** — 맵 + 스펙트럼 한 장 |
| `figures/Fig_leaf_maps.*` | 히트맵 8패널 (논문용) |
| `figures/Fig_leaf_spectra.*` | 대표 스펙트럼 2패널 (논문용) |
| `figures/Fig_leaf_contrast.*` | 대조군 대비 2패널 (논문용) |
| `figures/CAPTION.md` | 캡션 원문 |
| `data/map_pixels_long.csv` | 픽셀 1600줄 × 전 판독값 — Origin long form |
| `data/matrix_<시료>_<판독값>.csv` | 격자 행렬 — Origin heat map 용 (행렬이라야 만든다) |
| `data/spectra_representative.csv` | 파장수 × (시료별 중앙값·q25·q75·잉크정규화·대조군차감) |
| `data/summary_stats.csv` | 시료×판독값 중앙값 / `within_prep_SD` / CV / p10 / p90 |
| `data/detection_vs_control.csv` | 대조군 대비 배수 · SNR · 3 SD 문턱 · 검출 픽셀 비율 |
| `data/band_selectivity.csv` | 마커밴드 하나하나의 시료간 대비 (§4-3 표) |
| `data/peel_removal.csv` | peel 전후 절대 제거율 + 잉크 대비 잔존 (§3-3) · 잉크 분위수별 제거율 (§3-4) |
| `scripts/01_extract.py` · `02_figures.py` | 번호 순 실행 |
