# TODO

_기준일: 2026-08-04. 상세 근거는 `PAPER_WRAPUP_2026-08-04.md`._
_2026-08-14 에 §0 (고농도 라벨 정정) 이 맨 앞에 붙었다._

---

## 0. 🔴 고농도 라벨 정정 — 2026-08-14

`Ratio/260814_mixture_final/` (전 102맵을 최종농도 이름으로 정리한 폴더)과 대조해서
2026-08-12~13 의 export 를 검산했다. 경위와 새 숫자는 `FINDINGS_2026-08-12.md` 머리말과
`documentation/scripts/mixtures.py` 머리말에 있다.

### 무엇이 틀렸었나

1. **고농도 32조건의 µM 이 전부 원액 기준**이었다 — 등량 혼합이라 최종은 이성분 ÷2 ·
   삼성분 ÷3 인데 저농도만 ÷3 을 했다. 조성 %는 무사, **µM 축만 2–3배 과대**.
2. `Ratio_mix/DQ1000` 은 순물질이 아니라 **DQ:THI 1:1**(최종 `DQ500-TB0-TH500`).
   이 한 맵이 §3-1 음성 대조의 결론을 뒤집고 있었다.
3. `BAD` 로 빼 둔 2맵은 라벨이 맞아 보여 되살렸다 → 고농도 **32 → 34조건**.
4. `export_rsd.py` 가 격자 재추정을 이름으로 찾다가 `tert-new` 15맵을 놓쳐,
   **한 조건 안에서 두 추정기가 섞인 채** RSD 를 재고 있었다.

### 코드 — ✅ 끝

- [x] `mixtures.py` 신설 — 혼합물 목록·라벨 한 곳. 파일명이 곧 최종 µM, 나눗셈 없음
- [x] 수집 코드 3벌(`parse_hi`·`÷3`·`OLD` dict·`BAD`)을 쓰던 18개 스크립트를 전부 이관
- [x] `paths.py` 에 `DB`·`REPO` 탐색 추가 — 31개 파일의 맥 하드코딩 제거, **윈도우에서도 돎**
- [x] `paths.INTERP` + `INTERP_OUT` 환경변수 — 기존 결과를 덮어쓰지 않고 나란히 비교
- [x] 텍스트 `open()` 8곳에 `encoding="utf-8"` (윈도우 cp949 로 죽던 것)
- [x] `piemap_compute.py` 의 `makedirs` 를 앞으로 (4-fold 80분 돌고 저장에서 죽던 것)
- [x] `tert260805.py` 의 `Ratio/260805` → `Ratio/mis/tert-*` (폴더 이동으로 0맵이던 것)
- [x] `bench64.py` 의 체크포인트 정리 `rmtree(..., ignore_errors=True)` → 실패를 알리게.
      Drive 동기화가 파일을 잡고 있으면 조용히 실패해서 남은 폴더를 "아직 도는 중" 으로
      오해하게 된다 (2026-08-14 에 실제로 물려 다음 단계가 안 넘어갔다)

### 재실행 — 결과는 `ACF_PEST_DB/260814_final_rerun/`

- [x] `export_nnls.py --set low` · `export_lod` · `export_isotherm` · `gain_probe`
      → **어제와 바이트 동일** (저농도는 원래 맞았다는 확인)
- [x] `export_nnls.py --set high` → 34조건, NNLS 조성오차 30.5 → **29.1%**
- [x] `retrain_all` → `export_drift` → `export_negctrl`
      → THI 음성대조 최대 **45.2 → 0.6%**, 모든 문턱 오탐 0%
- [x] `conc38` → `export_rsd` → RSDr 중앙 DQ 26.7 · TBZ 58.1 · THI 20.7%
- [x] `bench64` (패널 d·e) → 검출 AUC 가 전 방법 0.90 → 0.93 으로 오르고 **순위가 바뀜**
      (PLS·NNLS 0.931 > RF 0.929 > MLP 0.921). 조성오차는 MLP 16.3% 로 여전히 최고 —
      캡션을 "조성은 MLP 가 최고, 존재 판정은 선형 방법과 차이 없음" 으로 고칠 것
- [x] `piemap_compute` (패널 g·i·j·k·l) → `deshrink` — 조성 12.7 → **13.1%** (중앙 13.3)
- [x] `curve64` — 출발점(고농도만) 24.4 → **22.2%**, 평평해지는 지점 24 → **16조건**
- [x] `tert260805` → `tert_nnls` → `export_tert` (패널 c)
- [x] `panels.py --size 3.4,3.3 --fontscale 0.6 --bare` — 그림 일괄
- [x] `export_origin.py` — Origin 용 CSV 미러
- [x] `train_physics_mlp.py` — 채택 구성 배포 모델 저장 (`model_physics_mlp.pkl`, 881s)

### 문서

- [x] `FINDINGS_2026-08-12.md` — 머리말 · §1 · §2 · §2-2 · §3-1 · §4 · §5 · §5-1 · §6 · 재현
- [ ] 🔴 **`CLAIM_nnls_vs_learned_2026-08-12.md`** — §5 가 크게 바뀌었다.
      저농도 학습 우위가 `+2.4%p · p=0.023`(유의) → `+2.0%p · 37/65 · p=0.159`(유의하지
      않음)이고 고농도 이원도 `p=0.28 → 0.68` 이다. **삼원 7조건(+17.0%p · p=0.047)만
      남는다.** "저농도에서도 학습이 낫다" 는 문장을 지우고, 근거를 ① 고농도 삼원 ②
      절대농도 판독 ③ 검출은 대등(AUC NNLS 0.931 · MLP 0.921) 로 다시 세울 것
- [ ] `RESULTS_lowconc_2026-08-06.md:401` — "전부 최소 한 성분 300 µM 이상" → **150 µM**
- [ ] `RESULTS_lowconc_2026-08-06.md:4,389` — "Ratio_mix 34맵 (300–1000 µM)" → **3.3–500 µM**
- [ ] `SUMMARY_2026-08-11.md:21` — "고농도 32맵 (라벨 오류 2맵 제외)" → **34맵**
- [ ] `PAPER_WRAPUP_2026-08-04.md:199–201` · `SESSION_2026-08-04.md:171` — 표의 맵 이름이
      원액 기준(`TBZ1000THI10` 등)이다. 최종농도 이름으로 바꿀지 결정할 것

### 다음 세션

- [ ] **trio `06_anchor_competition.py` 회수율 재계산** — nominal 을 `Ratio_mix` 파일명
      µM(원액 기준)으로 썼다. 최종농도로 고치면 nominal ÷2(이원)·÷3(삼원) 이라 회수율이
      전부 2–3배 올라간다 (TBZ 중앙 0.16 → ~0.32 등). 방향(용량 의존 배제, THI 가 더 센
      억제자)은 유지 — 숫자만 다시. `mixtures.py` 로 라벨을 받게 고칠 것.
      글씨 맵 질량수지 µM(10.1/14.2/2.7)은 혼합 라벨을 안 쓰므로 **영향 없음**
- [ ] 🔴 **글씨 필름에서 새 모델들의 BLK 헤드가 왜 침묵하는가.** 세트 A 게이트(842px,
      발표와 일치)에 세 모델을 나란히 얹은 결과 (2026-08-15 01:19):
      ```
      260806 .dlm (발표)      DQ 14.1 TBZ 11.2 THI 12.6 BLK 62.1  → 재정규화 37/30/33 ✓
      blank,sips (채택)       DQ 63.0 TBZ 16.6 THI 20.4 BLK  0.0  ✗
      blank,ink,sips (ink-arm) DQ 53.0 TBZ 36.2 THI 10.7 BLK  0.0  ✗
      ```
      **ink 주입 가설은 반증** — 사전학습에 잉크를 넣어도(`sim_nuisance=True`) BLK 는
      그대로 0% 다. TBZ 는 올라오지만(16.6→36.2) THI 가 무너진다(20.4→10.7).
      다음 용의자: 전처리 차이 — 260806 .dlm 은 baseline off · trim 500–2500 ·
      log1p_raw 로 학습됐고 새 모델들은 baseline on · 전 구간이다. 필름 픽셀이 새
      모델의 blank 학습 분포 밖일 가능성. **당분간 글씨 맵 조성은 260806 .dlm 로 보고**
      (비율 학습이라 라벨 정정과 거의 무관 — 세트 A 재현 확인됨).
      µM 헤드는 구·신 모두 필름에서 무효(0.3/9.2/44.5 · 23/64/203 vs 질량수지
      10.1/2.7/14.2) — 필름 µM 은 질량수지 유지

- [ ] **글씨 맵("SERS", `Pest/260812_12 trio_THI_TBZ_DQ.csv`) 재판독** — 정정 라벨로
      학습해 저장한 `model_physics_mlp.pkl` 을 적용한다 (재학습 불필요, `apply_model`).
      기존 판독(구 라벨 모델: 조성 36.5/30.1/33.5 · DQ 10.1 · THI 14.2 · TBZ 2.7 µM,
      `analysis/trio-260812-concentration/README.md`)과 대조.
      **질문은 TBZ 도 올라오는가다** — 구 모델은 고농도 µM 라벨이 2–3배 부풀려진 채
      학습됐으니, 정정 모델에서 TBZ 가 12 µM 쪽으로 올라오면 2.7 의 상당 부분이
      라벨 인공물이었다는 뜻이고, 그대로 2.7 근처면 눌림이 진짜 표면 물리라는 뜻이다.
      DQ·THI 는 참값에 있었으니 안 움직여야 정상 — 같이 확인.
      가능하면 `dl_explain` 을 글씨 맵에도 걸어 TBZ VIP 절제로 "밴드 증거 vs 사전확률"
      을 가른다 (§2 글씨 시료 항목과 연결).
      ⚠ **막 얹으면 안 된다** (2026-08-14 확인): 전체 2400px 에 `apply_model` 만 하면
      배경 종이 65% 가 섞여 조성 64/21/15 · µM 23/454/556 같은 외삽 쓰레기가 나온다.
      발표 판독은 ① threshold 게이트로 글씨 획만(842px) ② µM 은 모델 헤드가 아니라
      main 의 `analysis/trio-260812-concentration/scripts/01–06` 질량수지 체인 ③
      Reference 템플릿 · trim 500–2500 · baseline off 다. **그 체인을 그대로 두고 조성
      입력 모델만 정정본으로 바꿔야** 라벨 몫이 분리된다

### 판단이 필요한 것

- [ ] 🔴 **"hybrid" 농도 보정 스크립트 복원 — 통일은 그 다음.** 어제의
      `conc_*_hybrid_origin.csv`(2배 이내 DQ 77 · TBZ 95 · THI 85%)를 만든 스크립트가
      없다(재현 불가 4호). est 크기로 보아 conc38 추정 + 등온선(K) 보정이다.
      2026-08-15 에 후보 하나를 **배제**했다: MLP 농도 헤드가 아니다 —
      `conc_hybrid.py` 로 같은 held-out 재측정 시 DQ 56 · TBZ 56 · THI 22% 로 conc38
      보다 나쁘고 THI 를 2–3배 과소평가한다 (µM 보고에 MLP 헤드 금지).
      복원 전까지 **µM 보고값은 conc38** (재현 확인됨: DQ 65 · TBZ 87 · THI 89%).
      논문 패널 e 의 85/95/77 숫자를 쓰려면 이 스크립트부터 찾거나 다시 만들어야 한다
- [ ] 위와 별개로 **게인 산포 3.8배가 세 성분 공통의 천장**이다 (오라클 R² 0.61/0.79/0.82).
      잉크 2137 밴드가 이미 그 90% 라 통계로는 더 못 메운다 — 기판 밝기를 직접 재는
      측정 설계가 있어야 한다 (§2 ③)

- [ ] **BAD 2맵을 계속 살릴 것인가.** 이름·md5 는 일관되는데 `DQ500-TB50-TH0` 의 NNLS
      오차가 65% 로 크다. 라벨이 아니라 측정 문제일 수 있다
- [ ] **등몰 계열 8맵**을 학습셋에 넣을 것인가 (지금은 §6 판별 장치라 계속 제외)
- [ ] `Ratio/260814_mixture_final/DQ500-TB0-THI5.csv` → `…-TH5.csv` 로 개명
      (코드는 받아 주지만 규약에서 벗어난다)
- [ ] `DQ333-TB333-TH333 (2).csv` — 이름이 겹치고 공백·괄호가 들어간다. 두 세트에 같은
      조성이 있다는 뜻이니 별도 이름이 낫다
- [ ] **커밋 안 된 산출물이 계속 나온다** — 만든 스크립트가 저장소에 없는 것들:
      ① `lod_loq_inkbasis.csv` · `panel_b_langmuir_*_inkbasis.csv` (08-13 14:40) — 미해결
      ② `tert260805_nnls.npz` → `tert_nnls.py` 로 해결 (08-14)
      ③ `conc_*_hybrid_origin.csv` — `conc_hybrid.py` 로 MLP-헤드 가설은 **배제**했고
        원본 계산은 여전히 미복원. **다른 세션의 physics-rescue 작업 산출로 추정**
      ④ `export_hybrid_xai.py` 자체도 메인 클론에만 있었다 → 이관해 커밋 (08-14)
      ⑤ 메인 클론의 추적 안 되는 스크립트 11개 — 확인 결과 **다른 세션의 진행 중 작업**
        이다 (`physics_max_rescue_*` · `search_best_physics_rescue` 계열, 표면 물리
        제약 손실 실험, 08-14 14–16시 수정, `260814_mixture_final` 을 이미 라벨 권위로
        씀). **그 세션에서 커밋하도록 요청** — 여기서 건드리지 않는다
- [x] 결과 폴더 — **`260814_data interpret/` 로 새로 판다.** `260808_data interpret/` 은
      정정 **전** 판이라 그대로 둔다 (대조용). 덮어쓰면 최상위 고농도 삼각형 PNG 20장이
      옛 라벨로 남아 섞인다. `paths.INTERP` 기본값도 새 폴더로 바꿨다

---

## 1. ✅ LOD — 확정됨

`LOD = 3.3 · σ_blank / 기울기`, blank = **INK 맵**(종이+잉크, 분석물만 없음)을
**INK 포함 전체 템플릿**으로 NNLS 투영, 기울기는 포화 전 구간(0.1–10 µM) OLS,
σ는 맵 400픽셀 기준(`σ_pixel/√400`).

| | LOD | µg/L | 단일 픽셀이면 |
|---|---|---|---|
| DQ | **84 nM** | 15.5 | 1.69 µM |
| TBZ | **94 nM** | 18.9 | 1.88 µM |
| THI | **83 nM** | 20.0 | 1.67 µM |

세션 중 값이 흔들린 원인 세 가지 — blank를 BLK로 잡음, readout에서 INK 누락,
격자점(onset)을 LOD로 읽음 — 모두 정정. 검량선 세트는 `Pest/Standard/260729/`로 고정.

> 남은 선택지 하나: 규제치와 비교하려면 **전처리 배율**(시료 g → 용매 mL)이 필요하다.
> 현재 측정은 전부 표준용액이라 나뭇잎 단계에서 정해진다.

---

## 2. 남은 측정 — 2026-08-05 갱신

### 2-1. 🔴 먼저: 잉크 판독이 조성에 무관한가

> **실행 지시서: `RUN_ink-check_6maps.md`** — 6조합, 맵당 100점, 자족적으로 작성됨.
> 다른 세션에 그 파일만 줘도 왜·무엇을·어떻게가 전달된다.
> (아래 9조합 버전은 맵당 10점 기준의 초안이며, 100점이 가능하므로 6조합 안을 쓴다.)


잉크 검량 `log10(INK) = −2.01·log10(C) + 6.32` (R² 0.906, 1.3배/88%)은
**1:1:1 · 용액 혼합 · 한 기판**에서만 나왔다. 일반성 확인이 완전요인 투자의 전제다.

**만들 것 — 전부 최종 농도 µM, 모든 값이 완전요인 격자(3·6·12·24·48) 위에 있다:**

| | DQ | TBZ | THI | 총 |
|---|---|---|---|---|
| **A. DQ 편중 (4:1:1)** | 12 | 3 | 3 | 18 |
| | 24 | 6 | 6 | 36 |
| | 48 | 12 | 12 | 72 |
| **B. 기준 (1:1:1)** | 6 | 6 | 6 | 18 |
| | 12 | 12 | 12 | 36 |
| | 24 | 24 | 24 | 72 |
| **C. THI 편중 (1:1:4)** | 3 | 3 | 12 | 18 |
| | 6 | 6 | 24 | 36 |
| | 12 | 12 | 48 | 72 |

**9조합 × 기판 2개 = 18맵, 맵당 10점.** 이 9개는 나중에 125조합의 **부분집합**이라
버려지는 것이 없다.

**판별력** — 같은 총농도에서 두 가설이 예측하는 잉크 계수:

| 총농도 | A (DQ 편중) | B (기준) | C (THI 편중) | A/C |
|---|---|---|---|---|
| 18 µM | 189,000 | 57,000 | 30,800 | **6.1배** |
| 36 µM | 46,900 | 14,200 | 7,640 | **6.1배** |
| 72 µM | 11,700 | 3,510 | 1,900 | **6.1배** |

```
잉크가 총농도만 읽으면    →  세 조성의 잉크가 같게 나온다
잉크가 유효피복(ΣKC)을 읽으면 →  위 표대로 6.1배 벌어진다
```

측정 산포(2026-08-05 기판 간 1.05–2.6배)보다 크므로 확실히 갈린다.

**왜 이 조성인가** — K가 1 : 11.6 : 16.9이므로 같은 총농도라도 조성에 따라 ΣKC가 달라진다.
DQ 편중이 최저, THI 편중이 최고. TBZ 편중은 기준과 1.14배밖에 차이가 없어 정보가 적다.
**왜 농도 3점인가** — 2점이면 직선 가정만 되고, 3점이면 조성별 기울기 + 잔차를 볼 수 있다.

- **성립하면** → 잉크가 총피복을 읽는다 → **총농도(잉크) × 조성(MLP) = 개별 농도**가 열린다
- **실패하면** → 잉크는 조성별 검량이 필요. 완전요인 설계를 다시 짜야 함
- **여유 되면 글씨 시료를 비율 달리해 몇 장** — 도포 방식 전이(고려대 맵 검증)에 더해
  **TBZ ×4.5 눌림이 글씨 도포에서 상수인지**가 풀린다 (2026-08-14 보강). 상수로 확인되면
  도포 방식별 검량 상수가 되어 글씨 맵의 TBZ µM 을 보정해 보고할 수 있다. 지금은 글씨
  n=1 이라 "SERS 유효 표면적재량 2.7 µM" 으로 무보정 보고가 맞다
  (`analysis/trio-260812-concentration/`). 씻어낸 LC-MS 가 배제(표면에 없음)와
  증강 손실(있는데 조용함)을 가른다. XAI 의 TBZ VIP 절제도 부분적 판정 근거 —
  글씨 맵의 TBZ 30% 가 밴드 증거를 쓰는지, 사전확률을 뱉는지

### 2-2. 완전요인 (2-1 통과 후)

**최종 농도 기준, 로그 간격 5수준:**

```
각 성분 :  3 · 6 · 12 · 24 · 48 µM       (단계마다 2배, 1.20 dec)
조합    :  5³ = 125       (3수준 27 / 4수준 64 로 축소 가능)
총농도  :  9 – 144 µM
맵당    :  10점 권장 (5점이면 LOD 841 nM로 최저 수준이 3.6배밖에 안 뜸)
```

| 제약 | 확인 |
|---|---|
| 최고 조합에서 잉크 생존 | 총 144 µM → INK 예상 872 ✓ |
| 최저 수준이 LOD 위 | 3 µM / LOD(N=10) 0.595 µM = **5.0×** ✓ |
| 로그 간격 | 단계마다 0.61 dec 균등 ✓ |

> ⚠️ **선형 간격(5·15·25·35·45)은 피할 것** — 첫 단계 3.0배, 마지막 1.29배라 위쪽 세 점이
> 겹친다(25→45에서 잉크가 3.3배만 변함). 범위·개수는 같은데 정보량이 적다.

> ⚠️ **`equal_volume_mix`를 꺼야 한다.** 완전요인은 등량 혼합이 아니라 성분마다 농도를
> 따로 잡으므로 "나누기 3"이 성립하지 않는다. **파일명에 최종 농도를 그대로 쓸 것.**

> ⚠️ **최고 농도를 100 µM까지 올리지 말 것** (1/3/10/30/100 안). 최고 조합 총 300 µM에서
> 잉크가 죽어(예상 INK 200) 격자 한쪽 모서리가 판독 불가가 된다.

**이 한 설계가 동시에 푸는 것**: 비율·농도 얽힘(0/34) · 잉크의 조성 무관성 ·
삼성분 심플렉스 내부 · 저피복에서 DQ·TBZ 회복 · 경험적 정방향 모델 적합.

### 2-3. 나머지

| # | 실험 | 규모 | 무엇이 풀리는가 |
|---|---|---|---|
| 1 | 잉크 창 경계 — 37~111 µM 사이 | 3~4맵 | 잉크가 죽는 농도. "몇 µM까지"를 쓸 수 있게 됨 |
| 2 | 대표 8~10맵 반복 | 8~10맵 | 조성 오차에 측정 재현성 기반 오차막대 (34장 다 할 필요 없음) |
| 3 | DQ 표면 개질 (CNF 카복실레이트 등) | — | DQ 감도. 설계 원리 실증 데모 |

**기존 34맵은 다시 찍지 말 것** — 고피복 팔로서 경쟁흡착 결과를 담고 있고, 같은 농도에서
다시 찍어도 잉크는 여전히 죽어 있다.

## 3. 남은 코드 작업

- [x] **`dl_model.py` epoch 규칙** — 홀드아웃 epoch 선택이 34맵에서는 성립하지 않는다
      (검증 맵 7개, 시드별 75/2/61, 곡선이 잡음). LOO가 손해를 확인: 350 학습 19.1% vs
      선택된 6 epoch 22.8%. **고정 300으로 교체**, 선택기는 `epoch_diagnostics=True`일 때만
      곡선을 기록하는 진단용으로 남김
- [x] **`apply_model` 맵 µM 판독** — µM 헤드를 스펙트럼 길이(967)·조성 헤드 크기(256/64)로
      만들어 **크래시하고 있었다**(실제는 16특징·128/32). 픽셀 경로에 위임하도록 수정 —
      학습이 맵 중앙값 pooled loss였으니 그것이 원래 맞는 방식
- [ ] **농도 헤드 물리 사전학습** — 조성 헤드는 `simulate_mixtures`로 사전학습하지만 농도
      헤드는 안 한다. **단 지금은 붙이면 안 된다**: 시뮬레이터가 경쟁 Langmuir 정방향인데
      2026-08-05 실험이 그 모델을 반증했다(조성 불변 예측 실패). **틀린 정방향으로 만든 합성
      데이터는 틀린 역함수를 가르친다.** 완전요인 → 경험적 정방향 모델 적합 → 그 다음
- [ ] **Real 탭을 논문 주장에 맞추기** — 농도 복원이 검증되지 않았는데 µM을 그대로 표시함.
      경고 표기 또는 기본 숨김
- [x] `dataset.discover_references` — `_corrected.csv` 하나로 나머지 레퍼런스가 사라지던 문제.
      물질별 우선순위로 변경 (`dataset.py`)
- [x] `page_quantify` LOD 기울기 — 하드코딩된 "최저 5개 농도" 대신 실측 반응 구간에서 적합

---

## 4. 참고: sweat 논문의 정량 방법과 무엇이 다른가

> Park, Heo, Choi, **Lee** et al., *Wearable plasmonic SERS patch for quantitative sweat
> metabolite profiling via dual-track spectral inference* (revised manuscript).
> 본인이 공저자이므로 방법론을 그대로 인용해 설계 근거로 쓸 수 있습니다.

### 그 논문이 한 것

| 항목 | 내용 |
|---|---|
| 대상 | urea (0.5–10 mM) · uric acid (5–100 µM) · paraxanthine (1–50 µM) |
| **설계** | **완전요인 5수준 × 3물질 = 125 혼합물.** 각 물질을 **독립적으로** 변화 |
| 밴드 선택 | 구조적으로 귀속 가능 · 전 구간 위치 안정 · **단조 세기 증가** · 간섭과 최소 중첩 |
| 검량 | 각 물질 **자기 범위 안에서 선형**, R² **0.98 / 0.96 / 0.95** |
| 반복 | 검량점당 **5회**, 혼합물점당 **25회** |
| 모델 | dual-track — 고농도(urea·uric acid)는 1D ResNet 직접 회귀, 미량(paraxanthine)은 **unmixing 후 잔차 스펙트럼에 CNN + Huber 앙상블** |
| 전처리 | 1차 미분 평활 + 표준화 |
| 결과 | R² ≈ 0.99 (urea·uric acid), **0.89** (paraxanthine) |

### 우리 것과의 차이 — 셋 다 설계 차이지 모델 차이가 아님

| | sweat 논문 | pesticide (현재) |
|---|---|---|
| **농도 축** | 조성과 **직교** (완전요인) | 조성과 **완전히 얽힘** — 같은 비율을 다른 농도에서 찍은 맵 **0/34** |
| **측정 구간** | 각 물질 **선형 구간 안**, R² 0.95–0.98 | **포화 구간**. THI 반포화 31.7 µM인데 혼합물 3.3–500 µM |
| **혼합물 반복** | 점당 25회 | **없음** |
| 샘플 수 | 125 (3D 공간을 채움) | 34 (대부분 심플렉스 모서리, 삼성분 7개) |
| 밴드 선택 | 단조성 + 최소 중첩 기준 | VIP (같은 발상) — **여기는 일치** |

### 왜 그쪽은 되고 여기는 안 되나 — 문제의 종류가 다름

| | sweat 논문 | pesticide |
|---|---|---|
| 어려움의 정체 | **masking** — paraxanthine 신호가 urea에 **묻힘** | **saturation** — 표면이 차서 신호가 **안 변함** |
| 복원 가능? | **가능.** 신호는 있고 큰 신호 아래 있을 뿐 → unmixing + 잔차 CNN이 파냄 | **불가능.** 전달 단계에서 정보가 사라짐 |

**묻힌 건 파낼 수 있지만 애초에 안 실린 건 못 파냅니다.** dual-track이 대단해서가 아니라
**데이터에 정보가 있었기 때문에** 작동한 것입니다. 같은 프레임워크를 이 데이터에 씌워도 결과는 같습니다.

물리도 다릅니다 — urea/uric acid/paraxanthine은 약하게 붙는 소분자라 단분자층 포화까지 안 갑니다.
여기는 thiram이 S로 화학흡착해 표면을 점령합니다 (K가 diquat의 18배).

### 그대로 가져올 레시피

1. 각 물질의 **선형 구간 확인** — 여기선 이미 완료: DQ 100–1000, TBZ 0.1–50, THI 0.5–5 µM
2. **각자 자기 구간 안에서 완전요인** — 3수준×3물질 = 27맵이 최소 (그 논문은 5수준 125맵)
3. **조건당 반복 ≥3회**
4. 밴드 선택 기준은 이미 동등 (VIP ≈ 단조성 + 최소 중첩)

> ⚠️ **DQ가 걸림돌**입니다. 반응 구간이 100–1000 µM으로 1 dec뿐이라 수준을 나눌 여지가 좁습니다.
> §2-5(표면 개질)가 여기서 선택이 아니라 **전제**가 됩니다.

---

## 5. Real 탭 µM을 다시 켜려면 — 작업 농도 상자와 단계

### 지금 안 되는 이유는 농도대가 겹쳐서다

34맵은 세 물질을 비슷한 농도로 섞는다. 그러면 표면이 거의 꽉 찬다:

| 조건 | θ_DQ | θ_TBZ | θ_THI | **Σθ** |
|---|---|---|---|---|
| 167 µM씩 | 0.03 | 0.35 | 0.51 | **0.90** |
| 333 µM씩 | 0.03 | 0.37 | 0.54 | **0.95** |

포화라 농도를 올려도 θ가 안 움직이고, **DQ는 θ 0.03으로 표면에서 밀려난다**
(친화도가 17배 약한데 농도는 같으므로).

### 각자 자기 구간에 놓으면 풀린다

| 조건 | θ_DQ | θ_TBZ | θ_THI | Σθ | 응답 여력 |
|---|---|---|---|---|---|
| DQ 100 · TBZ 5 · THI 1 µM | 0.136 | 0.079 | 0.023 | **0.24** | **76%** |
| DQ 300 · TBZ 20 · THI 5 µM | 0.255 | 0.198 | 0.072 | 0.53 | 47% |

**권장 작업 상자** (각 약 1 decade, 셋 다 LOD의 10배 이상):

| | 하한 | 상한 |
|---|---|---|
| DQ | 30 µM | 300 µM |
| TBZ | 1 µM | 20 µM |
| THI | 0.2 µM | 5 µM |

sweat 논문도 같은 구조였다 — urea mM / uric acid µM / paraxanthine µM으로 **구간이
겹치지 않는데 각자 자기 범위에서 완전요인**을 돌렸다. 겹치지 않는 것이 문제가 아니라,
**겹치게 섞은 것**이 문제였다.

### 도달 가능한 목표 세 단계

| 목표 | 가능? | 조건 |
|---|---|---|
| **자릿수 반정량** | ✅ | 완전요인 27맵 + 위 작업 상자 |
| **2~3배 이내 µM** | 🟡 | 위 + 조건당 반복 ≥3회 |
| **±20% (SANTE 수준)** | ❌ | **내부표준 없이는 불가** — 기판 CV 37–50%가 천장 |

**설계로 풀리는 것**: 비율/농도 얽힘 · 포화 · DQ 밀림 → 재획득으로 해결
**설계로 안 풀리는 것**: 기판 gain 산포(CV 37–50%) → 내부표준 필요

→ Real 탭 µM은 **1단계에서 "semi-quantitative, order-of-magnitude" 라벨을 달고 재개 가능**.
2단계에서 라벨을 뗄 수 있고, 3단계는 기판 쪽 작업이 먼저다.

### 그 논문과 여기서 갈리는 지점 (§4와 별개로, µM 달성에 직결되는 것)

| | sweat 논문 | 여기 |
|---|---|---|
| **정규화** | **L2 정규화 적용** — 크기를 버리고 모양만 씀 | **L2 안 씀** — 농도가 크기에 있다고 보고 보존 |
| **평가 프로토콜** | 125조건 데이터에 대한 예측 대 실측. **held-out 분할이 본문에 기술돼 있지 않음** | **leave-one-map-out 34폴드** |
| 기판 | 수화 agarose 매트릭스 안에서 **in-situ 생성된 AuNP** (3D, 균질) | 종이 위 SERS ink (hot-spot 불균일, **CV 37–50%**) |
| 기판 CV | **본문에 없음** — 직접 비교 불가 | 37–50% (n=14) |
| 증강 | Monte Carlo 합성 4,000 + 실측 50× 오버샘플 | 물리 사전학습 (검량선 기반 시뮬레이션) — 같은 발상 |
| 학습 | LeakyReLU · AdamW · Huber · 300 epoch · batch 32 | ReLU · Adam · 가중 L1 · **고정 300 epoch** · 풀배치 |
| 후처리 | 예측값에 adaptive background subtraction | 없음 |

> ⚠️ **L2 정규화가 핵심 갈림길이다.** 그쪽은 크기를 버리고도 농도를 맞혔는데, 완전요인이라
> **각 물질의 비선형 응답이 서로 달라 모양 자체가 총량에 따라 변하기 때문**이다.
> 얽힌 설계에서는 이 경로가 막히므로 우리는 크기를 보존했고, 그런데 크기가 gain에 오염돼
> 있었다(총신호 vs 총농도 R² 0.02). **완전요인을 확보하면 두 경로가 다 열린다.**

> ⚠️ **R² 0.99를 우리 19.1%와 나란히 인용하지 말 것.** 프로토콜이 다르다 —
> 우리 쪽은 맵 단위 held-out이고, 그쪽 본문에는 분할 기술이 없다. 인용할 때는
> **설계(완전요인·반복)를 근거로 삼고 성능 수치는 나란히 놓지 않는 것**이 안전하다.

---

## 6. 1:1:1 희석 계열 — 오늘 측정분, 무엇을 보는가

기존 `TBZ1000DQ1000THI1000`(equal-volume 기준 333 µM씩)을 희석.

### 왜 이 세트가 판별 장치인가

1:1:1에서는 농도가 다 같이 줄면 분모가 상쇄된다:

```
θ_i / θ_j  =  K_i·C_i / (K_j·C_j)  =  K_i / K_j
```

→ **표면 조성이 희석과 무관하게 고정**된다. 예측치 **DQ 4.1 / TBZ 42.2 / THI 53.7%**.
즉 **조성에는 희석 정보가 0**이다. 모델이 이 맵들에 서로 다른 µM을 내놓으면 진짜로 크기를
읽는 것이고, 전부 같은 값을 내놓으면 조성만 읽고 있던 것이다. 34맵으로는 불가능했던 구분이
이 한 세트로 된다.

### 예측 (INK 포함 등온선의 K로부터)

| 희석 | 각 농도 | Σθ | LOD 여유 |
|---|---|---|---|
| ×1 (기존) | 333 µM | 0.946 | ✓ |
| ×1/10 | 33.3 µM | 0.638 | ✓ |
| ×1/30 | 11.1 µM | 0.370 | ✓ |
| ×1/100 | 3.33 µM | 0.150 | ✓ |
| ×1/300 | 1.11 µM | 0.055 | ✓ |
| ×1/1000 | 0.333 µM | 0.017 | ✓ (LOD의 3.7배) |
| ×1/3000 | 0.111 µM | 0.006 | ⚠ marginal — 여기까지 가지 말 것 |

농도 1000배에 Σθ는 **56배**만 변한다(위쪽이 포화로 압축). **×1/30 아래는 거의 비례**
(×1/100 → ×1/1000에서 농도 10배에 Σθ 8.8배) → **희석 쪽을 촘촘히 잡을 것.**

### 측정 후 확인할 것

1. **물리 검증** — 표면 조성이 정말 4.1 / 42.2 / 53.7%로 유지되는가.
   유지되면 K가 맞다는 **모델을 안 거친 독립 증거**. 변하면 단일성분 K가 경쟁 상황으로
   전이되지 않는다는 뜻이고, 그것도 결과다.
2. **모델 판별** — 이 5장에 대해 모델이 서로 다른 µM을 내놓는가. ← **본전**

---

## 7. 농도 헤드 물리 사전학습 (§6이 통과한 뒤)

조성 헤드는 이미 `simulate_mixtures`로 사전학습한다(`dl_model.py:358, 363`) — sweat 논문의
Monte Carlo 증강과 같은 발상, 같은 규모(5,000 vs 4,000). **그런데 농도 헤드(`make_conc_net`)는
사전학습을 쓰지 않는다.** 연결하면 되고, 근거가 있다:

`simulate_mixtures`가 **gain을 0.7–1.4로 흔든다**(`gain_lo/gain_hi`). 여기에 농도 헤드를
학습시키면 **모델이 gain으로 농도를 읽을 수 없게 강제되고**, 남는 길은 모양(shape)뿐이다.
모양은 물질마다 포화 지점이 달라 총량 정보를 담는다.

> **이것이 sweat 논문의 L2 정규화와 같은 전략이다.** 그쪽은 명시적으로 크기를 버렸고,
> 여기서는 gain jitter로 암묵적으로 버리게 만드는 셈이다. 코드는 이미 있다.

> ⚠️ **§6 없이는 붙여도 의미가 없다.** 34맵으로는 성공 여부를 판별할 수 없기 때문
> (농도 분산의 89%가 조성). 순서를 지킬 것.

**후처리(예측값 편향 보정)** 도 가능하다 — 계통 과소평가(DQ 0.23× / TBZ 0.25× / THI 0.48×)에
유효하다. 단 **fold 안에서 적합할 것.** 세션 초반의 "라벨범위 제약"이 정확히 이 함정이었다
(0.49 → 0.37 dec 개선이 사실은 모델에게 답의 위치를 알려준 결과).
