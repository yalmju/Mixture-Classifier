numpy
scipy
scikit-learn
matplotlib
PyQt6                  # UNMIXR native app (unmixr.py) + embedded matplotlib
# optional: ResNet1D deep detector — only needed for the "ResNet1D" model backend
torch
