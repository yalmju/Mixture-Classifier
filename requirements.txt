# UNMIXR native app (python UNMIXR.py) — core dependencies
numpy
scipy
scikit-learn        # also pulls in joblib (model save / load)
matplotlib
PyQt6               # native GUI + embedded matplotlib

# OPTIONAL — only for the "ResNet1D (torch)" model backend. Not required to run the
# app; install separately if you want that backend:
#   pip install torch
# (on Apple Silicon, first: python3 -m pip install --upgrade pip)
